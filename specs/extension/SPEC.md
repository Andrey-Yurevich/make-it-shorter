# Расширение Chrome

Manifest V3. Стек: React + TypeScript + Tailwind + shadcn/ui.

## Поверхности

| Поверхность | Что делает |
|---|---|
| **Side panel** | единственное место, где показывается результат: диалоги, кнопки, настройки, история |
| **Content script** | извлечение текста со страницы, плавающая иконка на выделении |
| **Service worker** | открытие панели, контекстное меню, горячая клавиша, welcome/uninstall, обновление конфига |

**Резюме не рисуется на странице.** Всё уходит в боковую панель. Одна поверхность вместо двух: не нужно бороться с чужим CSS на целой панели и дублировать рендер потока.

Единственное, что внедряется в страницу, — иконка на выделении. Она в Shadow DOM с обычным CSS: Tailwind в чужую страницу не попадает.

## Открытие панели

Три входа, все ведут в одну панель:

1. Клик по иконке расширения в тулбаре → `chrome.sidePanel.setPanelBehavior({openPanelOnActionClick: true})`.
2. Клик по плавающей иконке на выделении → content script шлёт сообщение, service worker вызывает `chrome.sidePanel.open({tabId})`.
3. Контекстное меню и горячая клавиша.

**Проверить на раннем этапе:** `chrome.sidePanel.open()` требует пользовательского жеста и вызывается только из service worker — content script этот API не видит. Цепочка «клик в странице → сообщение → открытие» работает, но укладывается в окно жеста. Если сорвётся, запасной путь — открывать панель только по иконке в тулбаре, а плавающую иконку использовать для передачи текста в уже открытую панель.

Манифест: разрешение `sidePanel`, секция `side_panel` с `default_path`.

## Плавающая иконка на выделении

Появляется рядом с выделением по `mouseup`, если выделено больше минимума символов. Прячется при снятии выделения, прокрутке и Esc.

Размер 28×28, Shadow DOM, `z-index` у верхней границы, позиция считается от `getBoundingClientRect()` выделения с поправкой на края вьюпорта.

Не показывается в полях ввода и `contenteditable` — там выделение обычно означает правку, а не чтение.

## Извлечение текста

| Режим | Источник |
|---|---|
| Выделение | `window.getSelection()` |
| Вся страница | Readability.js (Mozilla) |
| Ручной ввод | textarea в панели |

Нормализация: схлопывание пробелов и переводов строк, удаление повторяющихся блоков, обрезка до максимума из конфига.

При обрезке — пометка в панели, что обработана часть текста.

## Проверка длины

Единственная проверка перед отправкой: длина после нормализации в пределах `[minInput, maxInput]` из конфига. Пороги на старте — 50 и 30 000.

Проверок на «осмысленность текста» нет. Определять, есть ли смысл в тексте, — работа модели, а не регулярных выражений: она же решает, предлагать ли кнопки.

Клиентская проверка не должна быть строже серверной: иначе расширение отклонит текст, который сервер бы принял.

## Side panel

### Структура

```
┌─────────────────────────────┐
│ ☰  make it shorter          │  бургер + заголовок
├─────────────────────────────┤
│                             │
│   диалог: текст → резюме    │  прокручиваемая область
│   → кнопки → раскрытия      │
│                             │
├─────────────────────────────┤
│ [textarea] [▶]              │  ручной ввод
│                     ★★★★★   │  оценка, справа внизу
└─────────────────────────────┘
```

### Бургер-меню

- список предыдущих диалогов, новые сверху;
- «новый диалог»;
- удаление одного диалога и очистка всей истории;
- шестерёнка → настройки.

### Настройки (под шестерёнкой)

| Настройка | Значения | По умолчанию |
|---|---|---|
| Язык резюме | из списка поддерживаемых | язык интерфейса браузера |
| Степень сжатия | `light` / `normal` / `tight` | `normal` |

Остаток дневной квоты не показывается — на старте это лишний элемент. Про лимит пользователь узнаёт, только когда упрётся.

## История диалогов

Хранится в `chrome.storage.local`, на устройстве пользователя. На сервер не уходит и там не хранится.

| Параметр | Значение |
|---|---|
| Хранится | исходный текст, резюме, ID кнопок, ответы на них, время |
| Лимит | последние 50 диалогов |
| Вытеснение | старые удаляются автоматически |
| Удаление | по одному и всё сразу, из бургер-меню |

Разрешение `unlimitedStorage` не запрашивается: 50 диалогов укладываются в стандартную квоту, а лишнее разрешение удлиняет ревью.

**Формулировка в описании и в интерфейсе.** Обещание «ваш текст не сохраняется» относится к серверу. История лежит локально, и текст в магазине должен говорить именно это: не уходит с вашего устройства и не хранится у нас. Иначе получается неточность, за которую справедливо прилетит в отзывах.

## Сетевой запрос

Делается **из side panel**, не из content script и не из service worker.

Панель — обычный документ, живущий, пока открыта, и стрим приходит туда же, где рисуется. Service worker для этого не годится: Chrome выгружает его при простое.

Content script занимается только извлечением текста и передаёт его в панель сообщением.

## Обработка потока

| Событие | Действие |
|---|---|
| `delta` | дописать текст |
| `actions` | отрисовать кнопки; пустой массив — не рисовать ничего и не считать ошибкой |
| `done` | убрать индикатор |
| `error` | показать сообщение по коду |

Индикатор снимается только по `done` или `error`.

Ответ с кодом не 200 — транспортная ошибка: часть запросов отклоняет WAF и отдаёт обычный 403 без нашего протокола.

Таймаут 40 секунд без единого события → отмена, `upstream_error`. Держится выше серверного (30 с).

## Кнопки

Подпись берётся из `_locales/<lang>/buttons.json` по ID. ID без подписи молча пропускается — логировать это не нужно, читать такие логи некому.

Клик отправляет `/v1/expand` с исходным текстом, резюме и ID кнопки. Ответ стримится в тот же диалог под резюме.

## Виджет оценки

Пять звёзд в правом нижнем углу панели. При наведении подсвечиваются звёзды слева направо до текущей.

Клик → переход на `https://make-it-shorter.net/rate-us?stars=<1-5>`. Дальнейшая логика на стороне лендинга, расширение о ней не знает.

Скрывается навсегда после оценки или закрытия крестиком.

> **Риск, о котором стоит знать.** Развилка «низкая оценка → форма, высокая → магазин» называется review gating. Apple запрещает это прямо; у Chrome Web Store формулировка мягче, но практика попадает под манипуляцию рейтингом и служила основанием для снятия расширений. Безопасный вариант — вести всех в магазин, а форму обратной связи показывать рядом, а не вместо. Решение за вами, здесь оно зафиксировано как есть.

## Welcome и uninstall

| Событие | Действие |
|---|---|
| Установка | `chrome.runtime.onInstalled` с `reason === "install"` → открыть `https://make-it-shorter.net/welcome` |
| Удаление | `chrome.runtime.setUninstallURL("https://make-it-shorter.net/uninstall")` |

При обновлении расширения welcome не открывается — только при первой установке.

Сами страницы за рамками этой спеки, реализуются отдельно. Маршруты и заглушки — в спеке инфраструктуры.

## Настройки и хранилище

`chrome.storage.local`:

| Ключ | Значение | По умолчанию |
|---|---|---|
| `deviceId` | UUIDv4 | генерируется при установке |
| `lang` | код языка резюме | язык интерфейса, нормализованный |
| `ratio` | `light` \| `normal` \| `tight` | `normal` |
| `catalogVersion` | целое | из сборки |
| `config` | ответ `/v1/config` + время получения | встроенные значения |
| `history` | до 50 диалогов | пусто |
| `rated` | оценка поставлена или скрыта | false |

`chrome.storage.sync` не используется: квота привязана к устройству намеренно.

## Конфиг

`GET /v1/config` запрашивается при старте service worker и не чаще раза в сутки, кладётся в `chrome.storage`.

Оттуда: пороги длины, список языков, флаг доступности. Запрос не удался — работаем на встроенных значениях, пользователю ничего не сообщаем.

Флаг «сервис недоступен» — аварийный выключатель: панель показывает сообщение и не отправляет запросов.

## Локализация

**30 языков на старте:**

`en` `es` `zh-Hans` `zh-Hant` `hi` `ar` `pt-BR` `ru` `ja` `de` `fr` `ko` `it` `tr` `pl` `nl` `id` `vi` `th` `uk` `fa` `ms` `sv` `cs` `ro` `el` `hu` `da` `fi` `he`

Язык интерфейса браузера вне списка → английский.

Строки интерфейса — `chrome.i18n`, `_locales/<lang>/messages.json`.
Подписи кнопок — `_locales/<lang>/buttons.json`, ключи совпадают с ID каталога.

### Требования к качеству перевода

Подписи кнопок — самый заметный текст в продукте, и плохой перевод здесь виден сразу.

1. **Двойная проверка.** Перевод делается одним проходом, вычитывается вторым — независимым, без доступа к первому варианту. Расхождения разбираются вручную.
2. **Длина не больше 24 символов.** Длинный вариант переформулируется, а не обрезается.
3. **Никаких идиом.** «What's the catch» на английском хорошо, на 29 языках — плохо. Формулировки буквальные.
4. **Обратный перевод для проверки смысла**: перевод на язык → обратно на английский → сравнение с оригиналом. Расхождение смысла означает переделку.
5. Для `ar`, `fa`, `he` панель выставляет `dir="rtl"`.

Генерация подписей в рантайме запрещена — только заранее подготовленные файлы.

## UI-состояния панели

| Состояние | Вид |
|---|---|
| Пусто | приглашение выделить текст или вставить свой |
| Загрузка | скелет до первого `delta` |
| Стриминг | текст печатается, кнопок нет |
| Готово с кнопками | текст + ряд кнопок |
| Готово без кнопок | только текст |
| Раскрытие | ответ стримится под резюме, кнопка активна |
| Ошибка | сообщение + повтор, кроме `rate_limited` |
| Квота исчерпана | «лимит на сегодня исчерпан», без цифр |
| Сервис выключен | сообщение из конфига, ввод заблокирован |

## Логотип

Три горизонтальные полосы убывающей длины — текст, ставший короче. Читается на 16 пикселях, не требует деталей, не привязан к языку.

Исходник — `assets/logo.svg`. Растровые размеры для магазина генерируются из него.

## Сборка

Артефакт — zip для Chrome Web Store.

`catalogVersion` подставляется из `catalog.json` автоматически, руками не проставляется.

Типы SSE-событий пишутся здесь же, генерация из общей схемы не используется. Совместимость с сервером проверяется контрактным тестом на реальном ответе.

**Тест сборки:** у каждого ID из `catalog.json` есть подпись во всех 30 файлах `_locales/<lang>/buttons.json`. Не проходит — сборка падает.

Разрешения в манифесте:

```
permissions:      storage, contextMenus, activeTab, sidePanel
host_permissions: https://api.make-it-shorter.net/*
```

`<all_urls>` не запрашивать — удлиняет ревью.
